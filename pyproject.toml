[project]
name = "beauty-retail-bi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mimi", email = "hajimidb@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
beauty-retail-bi = "beauty_retail_bi:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
